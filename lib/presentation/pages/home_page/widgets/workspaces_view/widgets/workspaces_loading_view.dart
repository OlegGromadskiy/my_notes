import 'package:flutter/material.dart';

class WorkspacesLoadingView extends StatelessWidget {
  const WorkspacesLoadingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
